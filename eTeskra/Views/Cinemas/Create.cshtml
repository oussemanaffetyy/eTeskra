@model Cinema

@{
    ViewData["Title"] = "Create a new Cinema";
}

<section class="py-5">
    <div class="container py-5">
        <div class="row mb-4 mb-lg-5">
            <div class="col-md-8 col-xl-6 text-center mx-auto">
                <p class="fw-bold text-success mb-2">Add a New Cinema</p>
                <h2 class="fw-bold">Welcome</h2>
            </div>
        </div>
        <div class="row d-flex justify-content-center">
            <div class="col-md-6 col-xl-4">
                <div class="card">
                    <div class="card-body text-center d-flex flex-column align-items-center">
                        <div class="bs-icon-xl bs-icon-circle bs-icon-primary shadow bs-icon my-4">
                            <img id="LogoPreview" class="border-info rounded-circle" style="max-width: 100px" />
                        </div>
                        <form asp-action="Create" data-bs-theme="light">
                            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                            <div class="mb-3">
                                <input asp-for="Logo" class="form-control" type="text" placeholder="Logo URL" />
                                <span asp-validation-for="Logo" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <input asp-for="Name" class="form-control" type="text" placeholder="Cinema Name" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <input asp-for="Description" class="form-control" type="text" placeholder="Description" />
                                <span asp-validation-for="Description" class="text-danger"></span>
                            </div>

                            <div class="mb-3">
                                <button class="btn btn-primary shadow d-block w-100" type="submit">Create</button>
                            </div>
                            <div class="mb-3">
                                <a class="btn btn-secondary shadow d-block w-100" asp-action="Index">Show All Cinemas</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        $(document).ready(function () {
            var output = document.getElementById('LogoPreview');
            output.src = $("#Logo").val();
        });

        $("#Logo").on("change", function () {
            var output = document.getElementById('LogoPreview');
            output.src = $(this).val();
        });
    </script>
}
